query episodeDetails($episodeId: ID!) {
    episode(id: $episodeId)  {
        id
        name
        air_date
        episode
        characters {
            id
            name
            image
        }
    }
}