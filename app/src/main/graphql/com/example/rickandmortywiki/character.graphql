query character($id: ID!){
    character(id: $id) {
        name
        image
        status
        species
        gender
        origin {
            name
        }
        episode {
            id
            air_date
            name
            episode
        }
    }
}
