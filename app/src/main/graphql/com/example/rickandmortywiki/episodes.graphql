query episodes($pageNumber: Int!) {
    episodes(page: $pageNumber) {
        info {
            pages
            prev
            next
        }

        results {
            id
            name
            air_date
            episode
        }
    }
}